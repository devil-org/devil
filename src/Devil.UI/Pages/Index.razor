@page "/"

<UIPage @ref="_page" Title="Devil" Breadcrumbs="Models.BreadcrumbUtils.HomeBreadCrumbs">
    <Card>
        <CardHeader>Welcome</CardHeader>
        <CardBody>
            <CardText>Welcome to Devil</CardText>
        </CardBody>
    </Card>
</UIPage>



@code{
    #pragma warning disable CS8618 // Non-nullable field must contain a non-null value when exiting constructor. Consider declaring as nullable.
    [Inject] NavigationManager _navManager { get; set; }

    UIPage _page { get; set; }

#pragma warning restore CS8618 // Non-nullable field must contain a non-null value when exiting constructor. Consider declaring as nullable.

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            try
            {
                
            }
            catch (Exception ex)
            {
                this._page.SetErrors(new List<string> { ex.Message });
            }
            finally
            {
                this._page.SetLoading(false);
            }
        }
    }
}